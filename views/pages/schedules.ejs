<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>List of all schedules</title>
  </head>
  <body>
    <h1>Welcome!</h1>

    <% const daysOfWeek=['Monday', 'Tuesday' , 'Wednesday' , 'Thursday' ,
    'Friday' , 'Saturday' , 'Sunday' ] %>

    <h1>List of Schedules</h1>
    <table>
      <tr>
        <th>Name Surname</th>
        <th>Day</th>
        <th>Start Time</th>
        <th>End Time</th>
        <th>Date</th>
      </tr>
      <% for(let i=0; i < scheduleTable.length; i++) { %>
      <tr>
        <td>
          <a href="/users/<%=scheduleTable[i].user_id%>">
            ><%=scheduleTable[i].firstname%> <%=scheduleTable[i].surname %></a
          >
        </td>
        <td><%= daysOfWeek[scheduleTable[i].week_day -1] %></td>
        <td><%=scheduleTable[i].start_at %></td>
        <td><%=scheduleTable[i].end_at %></td>
        <td><%=scheduleTable[i].date %></td>
      </tr>
      <%} %>
    </table>

    <a href="/logout" class="button">Logout</a>
    <a href="/schedule-manager" class="button">Add a new schedule!</a>
  </body>
</html>
